def isleapyear(year):
    if not year.isdigit():
        return 0
    year = int(year)
    if year % 400 == 0 or year % 4 == 0 and year % 100 != 0:
        return True
    else:
        return False

y = str(input())
print(y,isleapyear(y))
